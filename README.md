# スライムこうぼう（PC側）

Claude Code が いま なにを しているかを、スライムが はたらく姿で見せます。
セッション 1つが スライム 1ぴき。何をしているか、返事を待っているかが 一目で わかります。

- **PCのブラウザ**で見る（この一式だけで動きます）
- **Androidのかべがみ**で見る（別途アプリ。ホーム画面で いつも動きます）

---

## 導入（AIに やってもらう場合）

このページを AI に読ませて「導入して」と頼めば そのまま進められます。
以下は AI 向けの 手順です。

### 1. 置く

このリポジトリを 使いやすい場所に クローンします。

```bash
git clone https://github.com/nekozuki0627/slimekoubou.git
```

必要なものは **Python 3.9 以上だけ**です。追加のライブラリは要りません。
（QRコードを出したい場合のみ `pip install segno`。無くても動きます）

### 2. フックを 登録する

`~/.claude/settings.json` に、同梱の `hooks.json` の中身を **足します**。
すでに入っている設定は **消さずに** マージしてください。
同名のイベントが既にある場合は、配列に 追記します。

これで Claude Code の 9つの出来事（ツール使用・許可待ち・停止など）が
ローカルの 8899 番に POST されます。**外部には 一切 送信されません。**

`hooks.json` の SessionStart には、定期タスクの一覧を読める場所へ写す一行も入っています。
Windows のストア版 Python からは `%APPDATA%\Claude` が見えないためです。

### 3. 起動する

```bash
python monitor.py
```

起動すると 画面に 2つの住所が出ます。

- `http://127.0.0.1:8899/` … このPCのブラウザで見る
- `http://<LAN IP>:8899/` … 同じWiFiのスマホから見る

### 4. パソコンを つけたら 自動で 起動するようにする

Windows なら スタートアップに ショートカットを置く、
mac / Linux なら launchd / systemd に登録する、などお好みで。

### 5. スマホのかべがみと つなぐ

ブラウザで開いた画面の **「スマホで見る」** を押すと QRコードが2枚出ます。

- 上（**かべがみアプリに つなぐ**）… スマホの普通のカメラで読むと、アプリが開いて住所が入ります
- 下（ブラウザで見る）… スマホのブラウザで そのまま見られます

### 6. 外出先からも 見たい場合

PCとスマホの両方に [Tailscale](https://tailscale.com/) を入れて 同じアカウントでログインします。
そのあと もう一度 QRを読むと、**家でも外でも 使える住所**（`100.` で始まるもの）に なります。

---

## スライム＝あなたが話しているセッション

時間で動く定期タスクも、実行されると Claude Code のセッションになります。
でも **スライムにはしません**。スライムは「あなたが話しているセッション」だけです。

見分けかたは 2段構えです。

**1. セッションの素性（どの人でも使えます）**

`~/.claude/sessions/*.json` の `kind` が `interactive` のものだけを人のセッションとみなします。
それ以外は自動とみなして外します。判定に使った素性は `_kinds.log` に書き出すので、
未知のものが出てきたら `HUMAN_KINDS` を見直せます。

**2. 会話の名前（目やす）**

素性がわからないぶんは、名前で判断します。

- 名前が 定期タスクの名前と一致する（`Morning weigh in` ↔ `morning-weigh-in`）
- 名前に `（自動` が入っている

2つめは **名づけのクセに頼った目やす**です。`monitor.py` の `TASK_WORDS` を
自分に合わせて書きかえてください。空 `()` にすれば 名前では判断しません。

かわりに、**次に動く予定**は部屋の下に小さく出ます。

## 本人でないと できないこと

ログインが要る／完全削除など権限外／物理的な行為／本人の判断——
そういう用件は、AIが自分で進められません。**同梱の `todo.md`** に書いておくと、
部屋の外に出ます。

```markdown
- [ ] クラウドのゴミ箱を空にする @me #けいり
```

- `- [ ]` が空いていて `@me` が付いている行だけ出ます
- `#うしろ` で どの仕事のものかを書けます（省略可）。同じ名前のスライムと同じ色の丸が付きます
- 画面の ✓ を押すと `- [x]` になります

台帳を **別の場所**（すでに使っているタスク表など）にしたい場合は、
`_config.json` を作って向き先を変えてください。**置き場を2つに分けると必ずズレます。**

```json
{ "todo_file": "~/Desktop/やること.md", "todo_mark": "@me" }
```

## おぼえておくこと

サーバーを入れなおしても パソコンを再起動しても、さっきまでのスライムが部屋に残ります。
`_state.json` に そのときのようすを書いています（このファイルは共有しません）。

## 仕組み

```
Claude Code ──(hook)──> monitor.py (localhost:8899) ──> ブラウザ / Androidアプリ
```

- `monitor.py` … Python標準ライブラリだけの HTTPサーバー。フックを受けて、いま動いているセッションの一覧を組み立てます
- `index.html` … ブラウザ用の画面（[kaplay](https://kaplayjs.com/) 製）
- セッション名は Claude Code の会話タイトルから、作業内容は使ったツール名から作ります

### 送っているもの・送っていないもの

- サーバーは **あなたのPCの中だけ**で動きます。外部のサーバーには 何も送りません
- 会話の中身は 送りません。表示に使うのは **タイトル**と、**直近の発言の要点（18文字まで）**だけです

---

## こまった時

| 症状 | みるところ |
|---|---|
| スライムが出ない | Claude Code のセッションが動いていますか |
| 「みつかりません」 | サーバーが起動していますか / 同じWiFiですか |
| スマホから繋がらない | ファイアウォールで 8899 が塞がれていませんか |

---

## 絵とライセンス

- 部屋・スライムの絵は **Arillo（堤田由麻）** が用意したものです。個人で使う分は 自由にどうぞ。再配布・商用利用は ご相談ください
- `lib/kaplay.js` は [kaplay](https://github.com/kaplayjs/kaplay)（MIT）です

作：Arillo
